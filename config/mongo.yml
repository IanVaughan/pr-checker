defaults: &defaults
  adapter: mongodb
  host: localhost
  # host: 127.0.0.1
  # port: 27017
  # options:
  #   w: 1
  #   pool_size: 1
  #   slave_ok: false
  #   ssl: false

development:
  <<: *defaults
  logger: mongo
  database: lab_stats_development

test: &test
  <<: *defaults
  database: lab_stats__test

production:
  <<: *defaults
  uri: <%= ENV['MONGOHQ_URL'] %>
  username: <%= ENV['MONGO_USERNAME'] %>
  password: <%= ENV['MONGO_PASSWORD'] %>
  database: lab_stats_production
